spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.H2Dialect

langchain4j:
  community:
    dashscope:
      chat-model:
        api-key: sk-test-key-for-testing
        model-name: qwen-math-turbo
      embedding-model:
        api-key: sk-test-key-for-testing
        model-name: text-embedding-v3
      streaming-chat-model:
        model-name: qwen-math-turbo
        api-key: sk-test-key-for-testing

bigmodel:
  api-key: test-api-key

chroma:
  url: http://localhost:8000
  collection-name: test_companion_memory

companion:
  user:
    name: TestUser
    profile: "测试用户"
  emotion-check-time: "22:00"